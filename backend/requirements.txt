# FastAPI Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.0

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP Client
httpx==0.27.2

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1

# Agent (Claude Code driver runs CLI, no SDK needed)
# The anthropic library is optional - only needed if you want
# to call Claude API directly in addition to Claude Code
# anthropic>=0.25.0
